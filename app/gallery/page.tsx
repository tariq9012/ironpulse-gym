'use client'
import { useState } from 'react'
import { PageShell, SectionHeading } from '@/components/ironpulse'
import { gallery } from '@/lib/ironpulse-data'
export default function GalleryPage(){const [filter,setFilter]=useState('All');const categories=['All',...gallery.map(x=>x[0])];return <PageShell><SectionHeading eyebrow="Inside IronPulse" title="The work, in motion." copy="A closer look at the people, spaces, and shared standard that make this more than a gym."/><div className="mt-12 flex flex-wrap gap-2">{categories.map(c=><button key={c} onClick={()=>setFilter(c)} className={`border px-4 py-3 text-xs font-bold uppercase tracking-[.1em] ${filter===c?'border-accent bg-accent text-background':'border-white/15 text-muted-foreground'}`}>{c}</button>)}</div><div className="mt-10 columns-1 gap-5 sm:columns-2 lg:columns-3">{gallery.filter(x=>filter==='All'||x[0]===filter).map(([cat,image,title])=><figure key={cat} className="group relative mb-5 break-inside-avoid overflow-hidden"><img src={image} alt={title} className="w-full grayscale transition duration-700 group-hover:scale-105 group-hover:grayscale-0"/><figcaption className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-background p-5 pt-12 text-xs font-bold uppercase tracking-[.14em]">{title}<span className="ml-3 text-accent">{cat}</span></figcaption></figure>)}</div><p className="mt-12 text-sm text-muted-foreground">Follow IronPulse <span className="text-accent">@ironpulse</span></p></PageShell>}
