'use client'
import Link from 'next/link'
import { useState } from 'react'
import { PageShell, SectionHeading } from '@/components/ironpulse'
import { programs } from '@/lib/ironpulse-data'
export default function ProgramsPage(){const [query,setQuery]=useState('');const filtered=programs.filter(p=>`${p.title} ${p.category} ${p.trainer}`.toLowerCase().includes(query.toLowerCase()));return <PageShell><SectionHeading eyebrow="The method" title="Train with intention." copy="No random workouts. No quick fixes. Choose the method that fits the way you want to move through the world."/><div className="mt-12 max-w-sm"><label className="sr-only" htmlFor="program-search">Search programs</label><input id="program-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search programs" className="w-full border border-white/15 bg-background px-4 py-4 text-sm outline-none focus:border-accent"/></div><div className="mt-14 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(p=><Link href={`/programs/${p.slug}`} key={p.slug} className="group border border-white/10 bg-background"><img src={p.image} alt={p.title} className="aspect-[4/3] w-full object-cover grayscale transition group-hover:grayscale-0"/><div className="p-6"><p className="text-xs font-bold uppercase tracking-[0.16em] text-accent">{p.category} · {p.difficulty}</p><h2 className="mt-4 font-display text-3xl font-black uppercase">{p.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{p.copy}</p><p className="mt-6 text-xs font-bold uppercase tracking-[0.14em]">{p.duration} · {p.trainer}</p></div></Link>)}</div></PageShell>}
