import Link from 'next/link'
import { notFound } from 'next/navigation'
import { ArrowRight } from 'lucide-react'
import { PageShell } from '@/components/ironpulse'
import { programs } from '@/lib/ironpulse-data'
export function generateStaticParams(){return programs.map(p=>({slug:p.slug}))}
export default async function ProgramDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=programs.find(item=>item.slug===slug);if(!p)notFound();return <PageShell eyebrow={`Program / ${p.category}`}><div className="grid gap-12 lg:grid-cols-[1.1fr_.9fr] lg:items-center"><div><img src={p.image} alt={p.title} className="aspect-[4/3] w-full object-cover"/></div><div><p className="eyebrow">{p.difficulty} · {p.duration}</p><h1 className="mt-5 font-display text-5xl font-black uppercase leading-[.9] tracking-[-.06em] sm:text-7xl">{p.title}</h1><p className="mt-7 text-base leading-7 text-muted-foreground">{p.copy}</p><p className="mt-7 text-sm">Led by <strong>{p.trainer}</strong></p><Link href="/free-trial" className="mt-9 inline-flex items-center gap-4 bg-accent px-6 py-4 text-xs font-bold uppercase tracking-[.16em] text-background">Book this program <ArrowRight size={16}/></Link></div></div><div className="mt-24 grid gap-12 border-t border-white/15 pt-12 md:grid-cols-3"><div><p className="eyebrow">Benefits</p><ul className="mt-5 space-y-3 text-sm text-muted-foreground">{p.benefits.map(b=><li key={b}>{b}</li>)}</ul></div><div><p className="eyebrow">Weekly schedule</p><p className="mt-5 text-sm leading-7 text-muted-foreground">Monday / Wednesday / Friday<br/>06:00, 08:00, 18:00<br/>Small groups, coach-led</p></div><div><p className="eyebrow">Investment</p><p className="mt-5 font-display text-4xl font-black">From $29</p><p className="mt-2 text-sm text-muted-foreground">Included with Pro membership.</p></div></div></PageShell>}
