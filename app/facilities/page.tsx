import { PageShell, SectionHeading } from '@/components/ironpulse'
import { facilities } from '@/lib/ironpulse-data'
export default function FacilitiesPage(){return <PageShell><SectionHeading eyebrow="The space" title="Built for performance." copy="Every corner of IronPulse is designed to remove friction between you and your strongest self."/><div className="mt-14 grid gap-5 md:grid-cols-2">{facilities.map(([title,copy,image],i)=><article key={title} className={`group relative min-h-[360px] overflow-hidden ${i===0||i===5?'md:row-span-2 md:min-h-[740px]':''}`}><img src={image} alt={title} className="absolute inset-0 h-full w-full object-cover grayscale transition duration-700 group-hover:scale-105 group-hover:grayscale-0"/><div className="absolute inset-0 bg-gradient-to-t from-background via-background/20 to-transparent"/><div className="relative flex h-full flex-col justify-end p-7"><p className="eyebrow">0{i+1}</p><h2 className="mt-3 font-display text-3xl font-black uppercase">{title}</h2><p className="mt-3 max-w-md text-sm leading-6 text-white/70">{copy}</p></div></article>)}</div></PageShell>}
