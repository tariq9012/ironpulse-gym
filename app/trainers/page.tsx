import Link from 'next/link'
import { PageShell, SectionHeading } from '@/components/ironpulse'
import { trainers } from '@/lib/ironpulse-data'
export default function TrainersPage(){return <PageShell><SectionHeading eyebrow="The coaches" title="Meet your coaches." copy="The right coach does more than count reps. They help you see what you are capable of."/><div className="mt-14 grid gap-5 md:grid-cols-3">{trainers.map(t=><Link href={`/trainers/${t.slug}`} key={t.slug} className="group border border-white/10 bg-background"><img src={t.image} alt={t.name} className="aspect-[4/5] w-full object-cover grayscale transition group-hover:grayscale-0"/><div className="p-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-accent">{t.specialty} · {t.experience}</p><h2 className="mt-3 font-display text-3xl font-black uppercase">{t.name}</h2><p className="mt-2 text-sm text-muted-foreground">{t.role}</p><div className="mt-5 flex flex-wrap gap-2">{t.tags.map(tag=><span key={tag} className="border border-white/15 px-2 py-1 text-[10px] uppercase tracking-[.1em] text-muted-foreground">{tag}</span>)}</div></div></Link>)}</div></PageShell>}
