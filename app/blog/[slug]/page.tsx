import { notFound } from 'next/navigation'
import Link from 'next/link'
import { articles } from '@/lib/ironpulse-data'
import { PageShell } from '@/components/ironpulse'
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=articles.find(a=>a.slug===slug);if(!article)notFound();return <PageShell eyebrow={`Journal / ${article.category}`}><article className="mx-auto max-w-4xl"><img src={article.image} alt={article.title} className="aspect-[16/8] w-full object-cover"/><p className="mt-10 text-xs font-bold uppercase tracking-[.16em] text-accent">{article.category} · {article.read}</p><h1 className="mt-5 max-w-3xl font-display text-5xl font-black uppercase leading-[.9] tracking-[-.06em] sm:text-7xl">{article.title}</h1><p className="mt-6 text-sm text-muted-foreground">By {article.author} · September 5, 2026</p><div className="prose prose-invert mt-12 max-w-2xl text-base leading-8 text-muted-foreground"><p>{article.excerpt} The best results rarely come from a single dramatic decision. They come from making the next useful choice often enough that it becomes part of who you are.</p><p>Start with a clear baseline, choose a small number of priorities, and give the process enough time to teach you something. Training is not punishment; it is practice for having more capacity in your life.</p><p>At IronPulse, we believe the goal is not to chase perfection. It is to build a standard you can return to, even when the week gets messy.</p></div><Link href="/blog" className="mt-12 inline-block border-b border-accent pb-3 text-xs font-bold uppercase tracking-[.16em]">Back to journal</Link></article></PageShell>}
