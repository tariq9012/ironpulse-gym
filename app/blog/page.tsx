'use client'
import Link from 'next/link'
import { useState } from 'react'
import { PageShell, SectionHeading } from '@/components/ironpulse'
import { articles } from '@/lib/ironpulse-data'
export default function BlogPage(){const [filter,setFilter]=useState('All');const cats=['All',...Array.from(new Set(articles.map(a=>a.category)))];const list=articles.filter(a=>filter==='All'||a.category===filter);return <PageShell><SectionHeading eyebrow="The journal" title="The IronPulse journal." copy="Practical ideas for training with more intention, recovering with more patience, and living with more capacity."/><div className="mt-12 flex flex-wrap gap-2">{cats.map(c=><button key={c} onClick={()=>setFilter(c)} className={`border px-4 py-3 text-xs font-bold uppercase tracking-[.1em] ${filter===c?'border-accent bg-accent text-background':'border-white/15 text-muted-foreground'}`}>{c}</button>)}</div><div className="mt-14 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{list.map(a=><Link href={`/blog/${a.slug}`} key={a.slug} className="group border border-white/10 bg-background"><img src={a.image} alt={a.title} className="aspect-[4/3] w-full object-cover grayscale transition group-hover:grayscale-0"/><div className="p-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-accent">{a.category} · {a.read}</p><h2 className="mt-4 font-display text-2xl font-black uppercase leading-tight">{a.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{a.excerpt}</p><p className="mt-6 text-xs font-bold uppercase tracking-[.12em]">Read article →</p></div></Link>)}</div></PageShell>}
