'use client'
import { useState } from 'react'
import Link from 'next/link'
import { PageShell, SectionHeading, FaqList } from '@/components/ironpulse'
export default function MembershipsPage(){const [cycle,setCycle]=useState<'monthly'|'quarterly'|'yearly'>('monthly');const mult={monthly:1,quarterly:.9,yearly:.8}[cycle];const plans=[['Basic',29,['Gym access','Locker','Basic equipment']],['Pro',59,['Full gym access','Group classes','Fitness assessment','Progress tracking']],['Elite',99,['Everything in Pro','Personal trainer','Nutrition plan','Priority booking']]];return <PageShell><SectionHeading eyebrow="Membership" title="Your next level awaits." copy="Choose the level of support that makes showing up feel inevitable."/><div className="mt-12 flex w-fit border border-white/15 p-1 text-xs font-bold uppercase tracking-[0.12em]">{(['monthly','quarterly','yearly'] as const).map(c=><button key={c} onClick={()=>setCycle(c)} className={`px-4 py-3 ${cycle===c?'bg-accent text-background':'text-muted-foreground'}`}>{c}</button>)}</div><div className="mt-12 grid gap-4 md:grid-cols-3">{plans.map(([name,price,features],i)=><div key={name as string} className={`border p-7 ${i===1?'border-accent bg-accent text-background':'border-white/15 bg-background'}`}><p className="text-xs font-bold uppercase tracking-[0.16em] opacity-70">{i===1?'Recommended':'Membership'}</p><h2 className="mt-12 font-display text-3xl font-black uppercase">{name}</h2><p className="mt-3 text-4xl font-black">${Math.round((price as number)*mult)}<span className="text-sm font-normal opacity-60"> / mo</span></p><ul className="mt-8 space-y-3 border-t border-current/15 pt-6 text-sm leading-6 opacity-75">{(features as string[]).map(f=><li key={f}>{f}</li>)}</ul><Link href="/free-trial" className="mt-9 block border-t border-current/20 pt-5 text-xs font-bold uppercase tracking-[0.15em]">Start with a trial</Link></div>)}</div><div className="mx-auto mt-24 max-w-3xl"><h2 className="font-display text-4xl font-black uppercase">Membership questions.</h2><div className="mt-8"><FaqList items={[["Can I pause my membership?","Yes. We offer flexible pauses for travel, recovery, or life changes."],["Is there a joining fee?","No hidden joining fee. We start with a complimentary intro so you know the fit."],["Can I change plans?","Absolutely. Our team can help you find the right level as your goals evolve."]]}/></div></div></PageShell>}
