'use client'
import { useState } from 'react'
import { PageShell, SectionHeading } from '@/components/ironpulse'
import { classes } from '@/lib/ironpulse-data'
export default function ClassesPage(){const [day,setDay]=useState('All');const days=['All','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];const rows=classes.filter(c=>day==='All'||c[0]===day);return <PageShell><SectionHeading eyebrow="The schedule" title="Train when it matters." copy="A weekly rhythm of coached sessions, open space, and room to build a practice that lasts."/><div className="mt-12 flex flex-wrap gap-2">{days.map(d=><button key={d} onClick={()=>setDay(d)} className={`border px-4 py-3 text-xs font-bold uppercase tracking-[0.12em] ${day===d?'border-accent bg-accent text-background':'border-white/15 text-muted-foreground'}`}>{d}</button>)}</div><div className="mt-10 overflow-x-auto"><div className="min-w-[680px] border-t border-white/15">{rows.map((c,i)=><div key={`${c[0]}-${c[1]}-${i}`} className="grid grid-cols-[1fr_1fr_1.4fr_1fr_1fr_auto] items-center gap-4 border-b border-white/10 py-5 text-sm"><span className="text-muted-foreground">{c[0]}<br/><strong className="text-foreground">{c[1]}</strong></span><strong className="font-display text-lg uppercase">{c[2]}</strong><span className="text-muted-foreground">{c[3]}</span><span className="text-xs uppercase tracking-[0.1em] text-accent">{c[4]}</span><span className="text-xs text-muted-foreground">{c[5]}</span><button className="border border-accent px-3 py-2 text-[10px] font-bold uppercase tracking-[0.12em]">Book</button></div>)}</div></div></PageShell>}
