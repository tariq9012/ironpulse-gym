'use client'
import { useState } from 'react'
import { PageShell, SectionHeading } from '@/components/ironpulse'
import { stories } from '@/lib/ironpulse-data'
export default function TransformationsPage(){const [filter,setFilter]=useState('All');const goals=['All',...stories.map(s=>s.goal)];return <PageShell><SectionHeading eyebrow="Member stories" title="Real people. Real progress." copy="There is no single before and after. There are the small decisions, repeated until they become a new standard."/><div className="mt-12 flex flex-wrap gap-2">{goals.map(g=><button key={g} onClick={()=>setFilter(g)} className={`border px-4 py-3 text-xs font-bold uppercase tracking-[.1em] ${filter===g?'border-accent bg-accent text-background':'border-white/15 text-muted-foreground'}`}>{g}</button>)}</div><div className="mt-14 space-y-20">{stories.filter(s=>filter==='All'||s.goal===filter).map((s,i)=><article key={s.name} className="grid gap-8 lg:grid-cols-[1.1fr_.9fr] lg:items-center"><div className="relative"><img src={s.image} alt={`${s.name} transformation`} className="aspect-[5/3] w-full object-cover"/><div className="absolute bottom-4 left-4 bg-background px-4 py-3 text-xs font-bold uppercase tracking-[.13em]">Before → After</div></div><div><p className="eyebrow">{s.duration} · {s.goal}</p><h2 className="mt-4 font-display text-4xl font-black uppercase">{s.name}</h2><p className="mt-5 text-sm uppercase tracking-[.12em] text-muted-foreground">{s.program}</p><p className="mt-7 max-w-md text-lg leading-8">“{s.quote}”</p></div></article>)}</div></PageShell>}
